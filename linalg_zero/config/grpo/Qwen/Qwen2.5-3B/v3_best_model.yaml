defaults:
  - /training@training: training_schema
  - /run@run: run_schema
  - _self_

# This file basically takes the absolute best SFT model for training

init:
  max_lora_rank: 32
  dtype: bfloat16
  load_in_4bit: false
  load_in_8bit: false
  # Keep this aligned with engine.max_model_len; we cap at ~5800 effective tokens
  # (5000 prompt + 800 completion), so 6144 (with buffer for special tokens) is enough and saves KV-cache memory.
  max_seq_length: 6144

# Note that effective batch size is 32 = trajectories_per_group * groups_per_step
training:
  trajectories_per_group: 4
  groups_per_step: 8
  learning_rate: 1e-6
  beta: 0.03
  eval_steps: 100
  num_epochs: 2
  train_mode: sync_rl
  chat_template: "{%- if tools %}\n    {{- '<|im_start|>system\\n' }}\n    {%- if messages[0]['role'] == 'system' %}\n        {{- messages[0]['content'] }}\n    {%- else %}\n        {{- 'You are a helpful assistant.' }}\n    {%- endif %}\n    {{- \"\\n\\n# Tools\\n\\nYou may call one or more functions to assist with the user query.\\n\\nYou are provided with function signatures within <tools></tools> XML tags:\\n<tools>\" }}\n    {%- for tool in tools %}\n        {{- \"\\n\" }}\n        {{- tool | tojson }}\n    {%- endfor %}\n    {{- \"\\n</tools>\\n\\nFor each function call, return a json object with function name and arguments within <tool_call></tool_call> XML tags:\\n<tool_call>\\n{\\\"name\\\": <function-name>, \\\"arguments\\\": <args-json-object>}\\n</tool_call><|im_end|>\\n\" }}\n{%- else %}\n    {%- if messages[0]['role'] == 'system' %}\n        {{- '<|im_start|>system\\n' + messages[0]['content'] + '<|im_end|>\\n' }}\n    {%- else %}\n        {{- '<|im_start|>system\\nYou are a helpful assistant.<|im_end|>\\n' }}\n    {%- endif %}\n{%- endif %}\n{%- for message in messages %}\n    {%- if (message.role == \"user\") or (message.role == \"system\" and not loop.first) or (message.role == \"assistant\" and not message.tool_calls) %}\n        {{- '<|im_start|>' + message.role + '\\n' + message.content + '<|im_end|>' + '\\n' }}\n    {%- elif message.role == \"assistant\" %}\n        {{- '<|im_start|>' + message.role }}\n        {%- if message.content %}\n            {{- '\\n' + message.content }}\n        {%- endif %}\n        {%- for tool_call in message.tool_calls %}\n            {%- if tool_call.function is defined %}\n                {%- set tool_call = tool_call.function %}\n            {%- endif %}\n            {{- '\\n<tool_call>\\n{\"name\": \"' }}\n            {{- tool_call.name }}\n            {{- '\", \"arguments\": ' }}\n            {{- tool_call.arguments | tojson }}\n            {{- '}\\n</tool_call>' }}\n        {%- endfor %}\n        {{- '<|im_end|>\\n' }}\n    {%- elif message.role == \"tool\" %}\n        {%- if (loop.index0 == 0) or (messages[loop.index0 - 1].role != \"tool\") %}\n            {{- '<|im_start|>user' }}\n        {%- endif %}\n        {{- '\\n<tool_response>\\n' }}\n        {{- message.content }}\n        {{- '\\n</tool_response>' }}\n        {%- if loop.last or (messages[loop.index0 + 1].role != \"tool\") %}\n            {{- '<|im_end|>\\n' }}\n        {%- endif %}\n    {%- endif %}\n{%- endfor %}\n{%- if add_generation_prompt %}\n    {{- '<|im_start|>assistant\\n' }}\n{%- endif %}\n"

run:
  project_id: run001-linalgzero-sft
  project: linalgzero-grpo
  base_model: "atomwalk12/LinalgZero-SFT"
  model: "atomwalk12/LinalgZero-SFT"
  dataset_path: "atomwalk12/linalgzero-grpo"
  env: linear_algebra
  model_provider: hosted_vllm
  user_model_provider: openai
  user_strategy: mathematician
  user_model: gpt-4o
  agent_strategy: tool-calling-rl
  temperature: 0.7
  top_p: 0.8
  repetition_penalty: 1.0
  task_split: test
  log_dir: rl_results
  skip_eval: False
  in_process: False
  max_assistant_turns: 5 # Can set to 4 for optimised trajectories
  max_completion_tokens: 800
  skip_special_tokens: False
  stop: [
    "<tool_response>",
    "</tool_response>",
    "User:",
  ]
  # val_task_ids: [1,2,3]
  seed: 42

peft:
  r: 32
  lora_alpha: 64
  use_gradient_checkpointing: "unsloth"
  target_modules: ["q_proj", "k_proj", "v_proj", "o_proj", "gate_proj", "up_proj", "down_proj"]
  random_state: 42

trainer:
  learning_rate: 1e-6
  # Note that per_device_train_batch_size must be divisible by num_generations.
  per_device_train_batch_size: 4
  gradient_accumulation_steps: 1   # if != 1, deadlock occurs
  num_generations: 2
  max_prompt_length: 5000 # We have a maximum of 4 tool calls (800 * 4) + system prompt and user query (1800) = 5000
  max_completion_length: 800 # Maximum number of tokens per completion
  report_to: ["wandb"]
  save_total_limit: 10
  seed: 42
  data_seed: 42
  max_grad_norm: 0.1

engine:
  gpu_memory_utilization: 0.33
  max_lora_rank: 32
  enable_sleep_mode: true
  max_model_len: 6144
  dtype: 'bfloat16'
  quantization: null
  load_format: 'auto'
  seed: 42
