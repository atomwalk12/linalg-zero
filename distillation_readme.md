0. git clone https://github.com/atomwalk12/linalg-zero.git
1. Make sure the correct config file is used in Makefile (i.e. vllm_qwen3_32b.yaml)
2. Install environment via make install
2. Copy env.sh (contains secrets for huggingface & distillation hubs)
3. Login to huggingface hub
4. Create the argilla key and apply it to env.sh
